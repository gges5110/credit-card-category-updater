{
  "name": "credit-card-category-updater",
  "version": "1.0.0",
  "main": "dist/category-parser.js",
  "scripts": {
    "build": "vite build",
    "build:tsc": "tsc",
    "start": "npm run build && node dist/category-parser.cjs",
    "dev": "ts-node src/category-parser.ts",
    "test": "vitest run",
    "test:unit": "vitest run src/category-parser.test.ts",
    "test:integration": "vitest run src/integration.test.ts",
    "preview": "vite preview"
  },
  "dependencies": {
    "node-fetch": "^2.7.0",
    "@types/node-fetch": "^2.6.0",
    "cheerio": "^1.0.0-rc.12"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "author": "",
  "license": "ISC",
  "description": "Parse quarterly categories from Discover and Chase Freedom credit cards"
}
